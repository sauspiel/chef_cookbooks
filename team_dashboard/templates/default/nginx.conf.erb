upstream unicorn_server_<%= @app_name %> {
  server unix:/<%= node[:unicorn][:rundir] %>/unicorn_<%= @app_name %>.sock fail_timeout=0;
}

server {
  listen 80;
  server_name <%= @domain %>;

  location / {
    include <%= "#{node[:nginx][:dir]}/common/proxy.conf" %>;
    proxy_pass http://unicorn_server_<%= @app_name %>;
  }
}
