# configured by chef for <%= @node[:fqdn] %>


local   all           postgres                                ident
local   all           all                                     trust
host    all           all             127.0.0.1/32            trust

<% @node[:postgresql][:networks].each do |n| %>
<%= n[:host] || 'host' %>    <%= n[:database] || 'all' %> <%= n[:user] || 'all' %>  <%= n[:address] || ''%> <%= n[:mode] || 'trust' %>
<% end %>
